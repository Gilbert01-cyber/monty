#include "monty.h"

int main(int argc, char *argv[])
{
    /* Check if user provided a file argument */
    if (argc != 2)
    {
        fprintf(stderr, "USAGE: monty file\n");
        exit(EXIT_FAILURE);
    }
    /* File opening and line processing logic will go here */
    return (0);
}
